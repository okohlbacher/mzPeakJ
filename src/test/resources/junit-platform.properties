# When the corpus root is absent (CI without local data), @MethodSource("corpusFiles") returns
# an empty stream. JUnit 5.11+ fails by default; SKIP reverts to the previous silent-skip behaviour.
junit.jupiter.params.arguments.source.empty.failure.mode=SKIP
